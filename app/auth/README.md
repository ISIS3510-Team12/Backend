## Auth
This folder is for all logic that is related to authentication and authorization using firebase authentication. This can include things like authentication services, authorization services, along with the verification of the JWT token and the extraction of the user information from it. 

This folder should be used by the API layer to handle authentication and authorization for the API endpoints.